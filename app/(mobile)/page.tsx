import { MovieCard } from '@/components/MovieCard';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Афиша'
};

const Posters = () => {
  return <main className='flex w-full flex-col items-center justify-center'></main>;
};

export default Posters;
